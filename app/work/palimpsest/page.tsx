import type { Metadata } from "next";
import { ButtonLink, PageShell, SectionLabel } from "@/components/site";
import { TrackVisit } from "@/components/TrackVisit";
import { MotionItem, MotionSection } from "@/components/motion";
import { CaseStudyMeta } from "@/components/CaseStudyMeta";
import { createMetadata } from "@/src/lib/seo";

export const metadata: Metadata = createMetadata({
  path: "/work/palimpsest",
  title: "Palimpsest — Verifiable Knowledge Projection",
  description: "Polylogue-backed projection runtime that produces sanitized Markdown with provenance, hashes, redaction checks, and fail-closed ledger verification.",
});

const chips = ["Polylogue", "Markdown projection", "Provenance", "Hash verification", "Redaction checks", "GitHub Actions"];

export default function PalimpsestPage() {
  return <PageShell><TrackVisit slug="palimpsest" />
    <section className="bg-dark text-light"><div className="mx-auto max-w-7xl px-6 py-14 sm:px-10 lg:px-16 lg:py-16"><SectionLabel>Case study / 06</SectionLabel><h1 className="display mt-6 max-w-5xl text-[clamp(2.95rem,5.7vw,5.75rem)] leading-[.92]">Conversation history as a<br /><span className="text-cyan">verifiable projection</span></h1><p className="mt-7 max-w-3xl text-lg leading-relaxed text-muted-dark">Palimpsest projects authoritative Polylogue conversation history into sanitized Markdown for downstream knowledge systems while preserving provenance, content hashes, redaction checks, and a fail-closed verification boundary.</p><CaseStudyMeta slug="palimpsest" /><div className="mt-7 grid gap-px border border-dark-line bg-dark-line sm:grid-cols-2 lg:grid-cols-4">{[["Type", "Knowledge projection"], ["Authority", "Polylogue ledger"], ["Projection", "Sanitized Markdown"], ["Verification", "Hashes + redaction + CI"]].map(([label, value]) => <div className="bg-dark p-4" key={label}><p className="font-mono text-[10px] uppercase tracking-[.12em] text-muted-dark">{label}</p><p className="mt-2 text-sm text-light">{value}</p></div>)}</div><div className="mt-7 flex flex-wrap gap-2">{chips.map((chip) => <span key={chip} className="border border-dark-line px-3 py-2 font-mono text-[11px] text-muted-dark">{chip}</span>)}</div></div></section>
    <section className="bg-light text-ink"><div className="mx-auto max-w-7xl px-6 py-12 sm:px-10 lg:px-16"><MotionSection className="grid gap-10 lg:grid-cols-[1fr_1fr]"><div><SectionLabel light>Authority split</SectionLabel><p className="mt-6 text-2xl leading-tight">Polylogue is authority. Palimpsest is a derived projection. Downstream memory systems consume the projection without becoming the source of truth.</p></div><p className="max-w-xl text-base leading-relaxed text-muted-light">The runtime exists to preserve useful conversational knowledge while keeping the source ledger, projection provenance, sanitization rules, and verification state inspectable.</p></MotionSection><MotionSection className="mt-10 border-y border-light-line py-7"><SectionLabel light>Projection path</SectionLabel><div className="mt-5 grid gap-px overflow-hidden border border-light-line bg-light-line sm:grid-cols-3 lg:grid-cols-5">{["Polylogue ledger", "Projection runtime", "Sanitize + redact", "Hash + provenance", "Downstream knowledge"].map((item, i) => <div key={item} className="bg-light p-4"><span className="font-mono text-xs text-blue">0{i + 1}</span><p className="mt-5 text-sm leading-tight">{item}</p></div>)}</div></MotionSection><MotionSection className="grid gap-10 py-12 lg:grid-cols-2"><div><SectionLabel light>Verification controls</SectionLabel><ul className="mt-6 space-y-3 text-base leading-relaxed"><li>Sanitized Markdown projection with provenance and content hashes.</li><li>Fail-closed ledger verifier.</li><li>Explicit redaction checks before downstream use.</li><li>Bounded unit/integration tests around projection and verification behavior.</li><li>GitHub Actions verification gate with pinned toolchain revisions.</li><li>Alternate ledger-root sync behavior covered by tests and fixes.</li></ul></div><div><SectionLabel light>Merged evidence</SectionLabel><p className="mt-6 text-2xl leading-tight">The initial Polylogue runtime/projection verification PR merged with 19 commits, 20 changed files, and +1,315 / -6 lines.</p><p className="mt-5 text-base leading-relaxed text-muted-light">The important result is not the line count. It is that the projection pipeline, ledger verification, redaction checks, integration tests, and CI boundary were merged as one inspectable runtime rather than left as an architectural promise.</p></div></MotionSection><MotionSection className="grid gap-10 border-t border-light-line py-12 lg:grid-cols-2"><div><SectionLabel light>Why it matters</SectionLabel><p className="mt-6 text-2xl leading-tight">Derived knowledge should be useful without pretending it is canonical truth.</p></div><div><ul className="mt-6 space-y-3 text-base text-muted-light"><li>Preserves authority hierarchy across conversation, projection, and memory systems.</li><li>Makes sanitization and redaction part of the pipeline rather than a manual afterthought.</li><li>Keeps provenance attached to projected knowledge.</li><li>Supports reproducible verification before downstream consumption.</li></ul></div></MotionSection></div></section>
    <section className="bg-blue text-white"><div className="mx-auto flex max-w-7xl flex-col gap-8 px-6 py-14 sm:px-10 lg:flex-row lg:items-end lg:justify-between lg:px-16"><MotionItem><h2 className="display max-w-3xl text-[clamp(2.8rem,5vw,5rem)]">Need knowledge without losing provenance?</h2></MotionItem><MotionItem><ButtonLink href="/about#audit" light>Book a systems audit</ButtonLink></MotionItem></div></section>
  </PageShell>;
}
