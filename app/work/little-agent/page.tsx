import type { Metadata } from "next";
import { ButtonLink, PageShell, SectionLabel } from "@/components/site";
import { TrackVisit } from "@/components/TrackVisit";
import { MotionItem, MotionSection } from "@/components/motion";
import { CaseStudyMeta } from "@/components/CaseStudyMeta";
import { createMetadata } from "@/src/lib/seo";

export const metadata: Metadata = createMetadata({
  path: "/work/little-agent",
  title: "little-agent — Governed Runtime",
  description: "A PostgreSQL-first governed runtime with canonical authority state, append-only events, transactional outbox, AGE/pgvector projections, and independent promotion gates.",
});

const chips = ["PostgreSQL 17", "Apache AGE", "pgvector", "Transactional outbox", "Append-only authority", "Independent promotion"];

export default function LittleAgentPage() {
  return <PageShell><TrackVisit slug="little-agent" />
    <section className="bg-dark text-light"><div className="mx-auto max-w-7xl px-6 py-14 sm:px-10 lg:px-16 lg:py-16"><SectionLabel>Case study / 04</SectionLabel><h1 className="display mt-6 max-w-5xl text-[clamp(2.95rem,5.7vw,5.75rem)] leading-[.92]">A governed runtime with<br /><span className="text-cyan">database-enforced authority</span></h1><p className="mt-7 max-w-3xl text-lg leading-relaxed text-muted-dark">little-agent is a production-design runtime where canonical state, authority events, projections, and promotion evidence are kept explicit instead of being delegated to agent memory or model output.</p><CaseStudyMeta slug="little-agent" /><div className="mt-7 grid gap-px border border-dark-line bg-dark-line sm:grid-cols-2 lg:grid-cols-4">{[["Type", "Governed runtime"], ["Canonical truth", "PostgreSQL"], ["Projections", "AGE + pgvector"], ["Status", "Tile 1 + Tile 2 promoted"]].map(([label, value]) => <div className="bg-dark p-4" key={label}><p className="font-mono text-[10px] uppercase tracking-[.12em] text-muted-dark">{label}</p><p className="mt-2 text-sm text-light">{value}</p></div>)}</div><div className="mt-7 flex flex-wrap gap-2">{chips.map((chip) => <span key={chip} className="border border-dark-line px-3 py-2 font-mono text-[11px] text-muted-dark">{chip}</span>)}</div></div></section>
    <section className="bg-light text-ink"><div className="mx-auto max-w-7xl px-6 py-12 sm:px-10 lg:px-16"><MotionSection className="grid gap-10 lg:grid-cols-[1fr_1fr]"><div><SectionLabel light>Authority model</SectionLabel><p className="mt-6 text-2xl leading-tight">PostgreSQL owns canonical truth. Graph and vector systems are rebuildable projections, not competing authorities.</p></div><p className="max-w-xl text-base leading-relaxed text-muted-light">The runtime uses relational identities, an append-only authority ledger, atomic business mutations, and a transactional outbox. AGE and pgvector support graph/semantic capabilities while remaining disposable and reconstructable from canonical state.</p></MotionSection><MotionSection className="mt-10 border-y border-light-line py-7"><SectionLabel light>Promoted foundation</SectionLabel><div className="mt-5 grid gap-px overflow-hidden border border-light-line bg-light-line sm:grid-cols-3 lg:grid-cols-6">{["PostgreSQL 17", "Checksum migrations", "Canonical identities", "Authority events", "Transactional outbox", "AGE + pgvector projections"].map((item, i) => <div key={item} className="bg-light p-4"><span className="font-mono text-xs text-blue">0{i + 1}</span><p className="mt-5 text-sm leading-tight">{item}</p></div>)}</div></MotionSection><MotionSection className="grid gap-10 py-12 lg:grid-cols-2"><div><SectionLabel light>Database controls</SectionLabel><ul className="mt-6 space-y-3 text-base leading-relaxed"><li>Ordered migrations are bound to exact SHA-256 checksums and serialized with an advisory lock.</li><li>Core repositories prove PostgreSQL CRUD, constraints, optimistic version rejection, and rollback.</li><li>Authority events are append-only with database-enforced UPDATE, DELETE, and TRUNCATE denial.</li><li>Business mutation + authority event + outbox enqueue are committed atomically.</li><li>Projection deletion/rebuild preserves canonical relational truth.</li><li>Real AGE traversal and pgvector cosine-similarity smoke tests exercise the projection boundary.</li></ul></div><div><SectionLabel light>Promotion discipline</SectionLabel><p className="mt-6 text-2xl leading-tight">Two candidates were rejected before Tile 2 was promoted.</p><ul className="mt-5 space-y-3 text-base text-muted-light"><li>First independent review rejected the candidate because the mandatory security suite was empty.</li><li>Second review rejected it because direct TRUNCATE enforcement proof was missing.</li><li>A third fresh review accepted only after packet, Mosaic, security, live, promotion, build-control, TCP-readiness, clean-tree, Docker-cleanup, and scope gates passed.</li></ul></div></MotionSection><MotionSection className="grid gap-10 border-t border-light-line py-12 lg:grid-cols-2"><div><SectionLabel light>Destructive-operation proof</SectionLabel><p className="mt-6 text-2xl leading-tight"><code>TRUNCATE governed_runtime.authority_events CASCADE</code> must fail.</p><p className="mt-5 text-base leading-relaxed text-muted-light">The direct lower-boundary test required PostgreSQL SQLSTATE <code>55000</code> and then re-verified that the authority ledger remained usable.</p></div><div><SectionLabel light>What this proves</SectionLabel><ul className="mt-6 space-y-3 text-base text-muted-light"><li>Authority can be enforced below the application layer.</li><li>Projection systems do not need to become canonical truth.</li><li>Independent rejection is useful evidence, not something to erase from the project story.</li><li>Promotion should require cumulative system proof, not just isolated green unit tests.</li></ul></div></MotionSection></div></section>
    <section className="bg-blue text-white"><div className="mx-auto flex max-w-7xl flex-col gap-8 px-6 py-14 sm:px-10 lg:flex-row lg:items-end lg:justify-between lg:px-16"><MotionItem><h2 className="display max-w-3xl text-[clamp(2.8rem,5vw,5rem)]">Need a runtime with enforceable authority?</h2></MotionItem><MotionItem><ButtonLink href="/about#audit" light>Book a systems audit</ButtonLink></MotionItem></div></section>
  </PageShell>;
}
